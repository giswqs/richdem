[build-system]
requires = ["setuptools>=69", "pybind11>=2.12", "numpy>=2.0"]
build-backend = "setuptools.build_meta"

[project]
name = "richdem2"
version = "2.4.3"
description = "High-Performance Terrain Analysis (maintained fork of RichDEM)"
readme = "README.md"
license = "GPL-3.0-only"
requires-python = ">=3.10"
authors = [
    {name = "Richard Barnes", email = "rbarnes@umn.edu"},
]
maintainers = [
    {name = "Qiusheng Wu", email = "giswqs@gmail.com"},
]
keywords = ["GIS", "terrain", "hydrology", "geomorphology", "raster"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Intended Audience :: End Users/Desktop",
    "Intended Audience :: Education",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Other Audience",
    "Natural Language :: English",
    "Topic :: Scientific/Engineering :: GIS",
    "Topic :: Scientific/Engineering :: Information Analysis",
    "Topic :: Scientific/Engineering :: Visualization",
    "Topic :: Software Development :: Libraries",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
]
dependencies = [
    "numpy>=1.24",
]

[project.optional-dependencies]
gdal = ["rasterio"]
test = ["pytest"]

[project.urls]
Homepage = "https://github.com/giswqs/richdem2"
Repository = "https://github.com/giswqs/richdem2"
Issues = "https://github.com/giswqs/richdem2/issues"

[project.scripts]
rd_depression_filling = "richdem.cli:DepressionFilling"
rd_breach_depressions = "richdem.cli:BreachDepressions"
rd_flow_accumulation = "richdem.cli:FlowAccumulation"
rd_terrain_attribute = "richdem.cli:TerrainAttribute"
rd_info = "richdem.cli:RdInfo"
rd_compare = "richdem.cli:RdCompare"

[tool.setuptools.packages.find]
include = ["richdem*"]

[tool.cibuildwheel]
build = "cp310-* cp311-* cp312-* cp313-*"
skip = "*-musllinux_* *-win32 *_i686"

[tool.cibuildwheel.macos]
archs = ["x86_64", "arm64"]

[tool.cibuildwheel.linux]
archs = ["x86_64"]

[tool.cibuildwheel.windows]
archs = ["AMD64"]
